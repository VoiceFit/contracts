# @voicefit 패키지는 GitHub Packages에서 받는다. 토큰은 환경 변수로만 넣는다 (파일에 적지 않는다).
#   로컬:  export NODE_AUTH_TOKEN=$(gh auth token)
#   CI:    NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
@voicefit:registry=https://npm.pkg.github.com
//npm.pkg.github.com/:_authToken=${NODE_AUTH_TOKEN}
